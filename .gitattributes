# CHANGELOG.md is owned by Release Please (GC-P027, ADR-063 2026-07-15 amendment).
# Only the release PR edits it, so there is no cross-PR line-range contention to
# mitigate; the former `merge=union` rule was removed because a union merge would
# duplicate release sections on the main -> dev back-merge.
